#include <string>

namespace NumberCounterParameters
{
    const std::string number_publish_frequency("/number_publish_frequency");
    const std::string number_to_publish("/number_to_publish");

} // namespace Parameters
